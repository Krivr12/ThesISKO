{
  "name": "thesisko",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "express-rate-limit": "^8.2.1",
    "node-cache": "^5.1.2",
    "svix": "^1.84.1"
  }
}
